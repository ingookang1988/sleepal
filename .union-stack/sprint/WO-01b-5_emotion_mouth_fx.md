---
id: WO-01b-5
title: 이산 표정·입·떠다니는 도상 — 트리거 수신부 + 아래꺼풀 + 입 + zzz/음표
status: Verifying
parent: PLAN-01b
evidence: "기존 표정 브랜치 face-sheet 증거를 가져오고 [CON-01b] 순수 payload 테스트·mobile 21/21·tsc를 통과. 현재 face-sheet 37개 assertion의 브라우저 실행과 미적 판단은 미완"
closed_by: []
---
# [WO-01b-5] 이산 표정·입·fx

**선행 [WO-01b-4].** 인간 결정(2026-08-22 대화): 표정 분석·제안 중 "①트리거 수신부 + ②아래꺼풀 + 입 + 특수 효과(zzz·음표)" 채택. [WO-02b-2] 옹알이(3감정 표정 동기)의 수신 측 선행 작업이다.

## 목표
[WO-01b-4]의 연속 표정(glare) 위에 **이산 표정(사건·봉투)**과 **입·도상 레이어**를 합성한다. 채널이 전부 0이면 b-4와 픽셀 단위로 동일 — 순수 추가.

## 구현 (2026-08-22)

### ① 이산 표정 트리거 수신부 — `face/expression.js`
- [CON-01b] `expr:trigger {kind, tone, source}` 를 palBus 에서 구독. 놀이 어휘는 **serve.js `PAL_SYSTEM` emotion enum 재사용**: `happy`(기쁨)·`curious`(호기심)·`sad`(서운함), 환경 반응은 `startled`·`relieved`. tone 은 0~1 수, 기존 babbleTone 문자열은 호환 입력으로 정규화한다.
- 봉투(atk·hold·rel): happy 1.9s · curious 1.6s · sad 2.6s. 끝나면 채널 전부 0 → 기하 산술적 원복.
- 채널 → 기하: happy = 아래꺼풀 0.62 + 입꼬리 ↑ / curious = 좌 +6%·우 −8% 높이 + 입 'o' / sad = **눈꼬리 −5°(찡그림과 부호 반대 = 바깥 처짐)** + 입꼬리 ↓.
- **EMO_GATE** — AWAKE 1 · DROWSY 0.5 · MORNING 1 · **ASLEEP/NIGHT 0**([CON-01b] 상태 게이트). NIGHT 는 `expression()` 최상단 R4 게이트가 EMO 자체를 버린다.
- 상태 전이 유발 0([CON-01] 규칙 2) — 시트 게이트로 고정.

### ② 아래꺼풀 — `face/eyes.js` `lowLidPath()` + 마스크 `lidL2/R2`
웃는 눈 ⌣ 은 위가 아니라 아래에서 만들어진다. 위꺼풀의 거울(윗변 가운데가 arch 만큼 올라옴). low=0 이면 눈 바닥 5mm 아래에 숨고 arch 도 0 — 눈에 닿는 경로가 없다.

### 입 — `face/mouth.js` (신규, H 소유)
- 곡선 1개(닫힘) + 타원 1개(하품·"후—") — **R1: 음소(viseme) 형태 없음.**
- 기하: 중심 (79, 63.5) · 폭 12~20mm · 눈 최저점(59.9)과 간격 게이트로 분리. 눈보다 얇고(3 vs 5) 옅게 — ASLEEP 감긴 눈 ⌣ 와의 혼동 완화(ASLEEP 입 opacity 0.28).
- 동기: 하품(DROWSY 주기 14~22s · MORNING 진입 1회, [WO-01b-3] 하품 오디오와 접점) · 안도 발화 시 "후—"(relief 엣지) · 움찔 시 다뭄(g 로 폭 축소) · 호흡 ±1% · 표류 절반 추종.
- ⛔ R4 — NIGHT 계산 없이 숨김.

### 특수 효과 — `face/fx.js` (신규, H 소유)
- **zzz**: ASLEEP 에서 오른눈 오른쪽(x≥143)에서 2.6~4.2s 간격 스폰, 동시 3개 상한, 위로 떠오르며 소멸(4.5s).
- **음표**: `expr:trigger {kind:'note'}` 1발 또는 음악 모드(setMusic) 자동 스폰 — 두 눈 사이 회랑(x 66~92)을 타고 오른다(3.6s). 발행자는 [WO-02c-1] 사운드 예정, 지금은 디버그·키(9·0) 경로.
- **R3 해석([ADR-119])**: `<text>` 아님 — `<path>`/`<use>` 도형이고 눈 영역 교차 0 을 게이트로 고정. R4 — NIGHT 진입 1프레임 안에 전부 제거.

### 수동 트리거 (발화자 부재 동안)
- 키: 6 기쁨 · 7 호기심 · 8 서운함 · 9 음표 · 0 음악 토글 — 전부 [CON-02] 버스 경유.
- 디버그 화면 버튼 5개(기쁨·호기심·서운함·♪ 음표·♪ 음악) — 누르면 얼굴로 돌아간 뒤 250ms 후 발사(디버그 화면은 얼굴을 대체하므로).

### 그 외
- `sw.js` 프리캐시에 `face/mouth.js`·`face/fx.js` 추가. BUILD `2026-08-22g`.
- HUD·계기판에 `emo`·`low` 채널 노출.
- 시트의 고정 900ms 타임아웃이 iframe 25개 × 모듈 10개에서 플레이키(실측 17/25) → **전 프레임 준비 폴링(상한 15s)** 으로 교체.

## 검사
기존 브랜치에서 기쁨·호기심·서운함·입·fx·R4를 브라우저로 검증한 자산을 가져왔다. 현재 시트는 조도 `startled/relieved`, 잘못된 payload, 소리 `curious`, ASLEEP note 차단을 포함한 **37개 assertion**을 가진다. 이 환경에서는 in-app Browser 제어 도구가 노출되지 않아 새 시트 실행은 미완이며, 순수 [CON-01b] 테스트와 모바일 계약 테스트로 비브라우저 부분을 고정했다.

## 규범 검토
- **R1** — 옹알이가 언어가 아니듯 입도 음소를 만들지 않는다(곡선+타원뿐). 발화 경로 게이트 유지.
- **R3** — 문자 게이트(textContent 0 · `<text>` 0) 그대로 통과. zzz 는 도상이며 눈 영역 밖 — 해석은 [ADR-119], 눈 교차 0 은 게이트.
- **R4** — 트리거·입·fx 삼중으로 각자 집행 + 시트 게이트 2개.
- **R5** — 서운함은 "시무룩·삐짐" 허용 범위([ARCH-01] R5), 소멸 없음.

## 남은 것
- [ ] **미적 판단 — 인간.** 입 곡률·굵기·위치, zzz·음표 크기·빈도, happy 아래꺼풀 깊이. 키 6~0 으로 즉석 확인 가능
- [ ] 폰 실기 미확인(이 머신 브라우저 창 비표시 — 검사는 전부 `SP._step` 결정적 경로)
- [ ] 색 튜닝은 여전히 [WO-01d-3] 이후(트레이싱지 통과 뒤)
- [ ] [WO-02b-2] 착수 시 babble.js 가 이 수신부로 발행 — kind 어휘는 [CON-01b] 표 참조
- [ ] 문서 후속: [PLAN-01b] v0.3(입 기하 반영)은 인간 승인 후
