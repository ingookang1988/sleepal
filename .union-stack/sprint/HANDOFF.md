<!-- [Wiki] 세션 이어달리기. 세션을 마치는 에이전트가 덮어쓴다.
     ⚠ 이 문서는 **포인터만** 담는다. 어떤 사실도 여기에만 있어서는 안 된다 —
     latest-only 이고 손으로 덮어쓰므로 병렬 세션이 서로를 지운다(2026-08-22 실제 발생).
     세부는 소유 WO·평면 문서에 두고 여기서는 ID로 가리킨다. -->
# Handoff → next session

## 1. Summary
**두 계보가 병렬로 돌았다.** 몸통([PLAN-01d] v0.5)은 접합부 누락이 드러나 기하를 t=2 로 재전개하고 재단 원형을 17쪽으로 재생성했다. 얼굴([PLAN-01b] v0.2)은 **코드 0줄에서 배포까지** 갔다 — 눈 5상태 · 표정 레이어 · BLE 배관. [CON-01]은 v0.2(메시지 추가 0, 해석·상수만).
사고 1건: 두 세션이 같은 ADR 번호를 동시 할당해 5개 id 가 두 결정씩 가리켰다 → **계보별 대역 분리**([ARCH-00] §ADR bands, [ADR-301]).

## 2. Changed locations (ID list)
- 얼굴: [PLAN-01b] · [WO-01b-1] [WO-01b-2] [WO-01b-4] · [CON-01] · [ADR-101]~[ADR-111]
- 몸통: [DOM-01] v0.5 · [PLAN-01d] v0.5 · [ADR-201]~[ADR-205]
- 공통: [ARCH-00](ADR 대역) · [PLAN-01a] v0.2(5Hz) · `verification/derived/state.md` · [WO-01e-2](배포 URL)
- 코드(신규): `app/index.html` · `app/face-sheet.html` · `scripts/serve.js` · `railway.json` · `.railwayignore` · `.claude/launch.json`
- 산출물: `cut-sheet.html`(17쪽) · `slippal-parts-a4.pdf`

## 3. Next task (single entry point)
**[WO-01d-1]** — 폰 실측 3종(외곽 · 화면 경계 · **케이스 포함 두께**). [PLAN-01d] 전체를 막고 있고 **유일하게 되돌릴 수 없는 계보**다. 이어서 [WO-01d-2] → [WO-01d-3].
얼굴 계보를 병행한다면 [WO-01b-3](오디오·백그라운드 재생 실측)이 다음이다.

## 4. Open / cautions
- **⚠️ [WO-01d-2] frontmatter 가 낡았다** — "18장"은 v0.4 값이고 v0.5 는 14품목 47장이다. 그리고 **앞·뒷면은 이미 재단됐는데 WO 에 기록이 없다**(남은 12품목 45장). 소유 세션이 갱신할 것
- 몸통 주의사항 전부(접합·강성·목공풀 시간·보강·발열·반사면) → **[PLAN-01d] §4·§7 · [DOM-01] v0.5**. 여기 옮겨 적지 않는다
- 인쇄 배율 100% + **눈금바 100mm 확인**이 1:1 의 유일한 현장 안전장치 → [WO-01d-2]
- 얼굴 미완: 폰 실기 확인 · 표정 상수(0.62 · 7°) 미적 판단 · **색 튜닝은 [WO-01d-3] 이후**(트레이싱지 통과 후) → [WO-01b-2] [WO-01b-4]
- **[CON-01]은 여전히 미검증 계약.** `toython_ble.ino` 는 한 번도 컴파일된 적이 없고 보드 왕복도 없다 — [WO-01a-1] · [PLAN-01a] 작업 2(12:20)가 첫 검증
- **ADR 번호는 자기 계보 대역에서만 할당한다** — 얼굴 101–199(다음 112) · 몸통 201–299(다음 206) · 그 외 301–399. 06–42 는 상류 인용이라 은퇴
- 인간 결정 1건 미해결 — [WO-01e-1] 3:00 연출(후보 3개)
- **전 문서 `tier: draft` — 규범적 효력 0.** [ARCH-01]을 검수 게이트로 쓰려면 인간 승격 필요
- 잔여: **git 커밋 0건**(`ref/**/source/` 16MB 포함 여부 미정 — 배포는 `.railwayignore` 로 이미 우회) · 루트 README 2종 템플릿 · 훅 미설치

## 5. Verification status
- 하네스: `node scripts/health.js` **게이트 전부 통과** · `zfs-linter` 통과 · ADR 중복 0
- 얼굴: `app/face-sheet.html` **14/14 OK**(R1 발화 0 · R3 문자 0 · viewBox 넘침 0 · Ø45/간격35/여백16.5 · 기하 45→17.13 · 눈꼬리 ±7.00 · **R4 밤 응답 0**). 시상수 실측 상승 0.632@150ms · 하강 0.368@2500ms. BLE 배관 11/11(라인버퍼·규칙 3·규칙 5). 배포 200 확인
- 몸통: 재단 원형 3단계 검증 전부 0(넘침·충돌·CSS mm↔viewBox 단위). **600dpi 래스터 실측만 미실시** — 이 머신에 래스터라이저가 없다. 최종 안전장치는 인쇄 후 눈금바 실측 → [DOM-01] §11
- **실기 검증은 전부 미완** — 폰 크롬 · 보드 · BLE 왕복 · 발열
